export const RELATIONSHIP_FUNNEL_V2_PAGE = String.raw`<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Evolution House — новая воронка отношений</title>
  <style>
    :root{color-scheme:light;--bg:#f5f6f4;--ink:#202420;--muted:#667067;--line:#d8ddd8;--green:#28634b;--rose:#a35d69;--white:#fff}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:14px/1.45 Inter,system-ui,sans-serif}main{width:min(100% - 32px,1180px);margin:28px auto 64px}header{display:flex;gap:20px;align-items:end;justify-content:space-between;margin-bottom:20px}h1{margin:0;font-size:24px;letter-spacing:0}p{margin:4px 0 0;color:var(--muted)}form{display:flex;gap:8px;align-items:end}label{display:grid;gap:4px;font-size:12px;color:var(--muted)}input,button{height:38px;border:1px solid var(--line);border-radius:4px;background:var(--white);padding:0 10px;font:inherit}button{border-color:var(--green);background:var(--green);color:#fff;font-weight:600;cursor:pointer}section{margin-top:26px}h2{margin:0 0 10px;font-size:17px;letter-spacing:0}table{width:100%;border-collapse:collapse;background:var(--white)}th,td{padding:10px 12px;border-bottom:1px solid var(--line);text-align:right;white-space:nowrap}th:first-child,td:first-child{text-align:left;white-space:normal}th{color:var(--muted);font-size:12px;font-weight:600;background:#fafbfa}.drop{color:var(--rose)}.good{color:var(--green);font-weight:600}.empty{padding:28px;background:var(--white);border:1px solid var(--line);color:var(--muted)}#status{min-height:20px;margin-top:10px}.scroll{overflow-x:auto;border:1px solid var(--line);border-radius:6px}@media(max-width:720px){main{width:min(100% - 20px,1180px);margin-top:18px}header{align-items:stretch;flex-direction:column}form{display:grid;grid-template-columns:1fr 1fr auto}h1{font-size:21px}th,td{padding:9px 10px}}
  </style>
</head>
<body><main>
  <header><div><h1>Воронка «Сначала канал»</h1><p>Meta и YouTube → лендинг → канал → тест → возвращение в канал</p></div><form id="filters"><label>С даты<input id="date-from" type="date" required></label><label>По дату<input id="date-to" type="date" required></label><button>Обновить</button></form></header>
  <div id="status"></div><div id="content"></div>
</main><script>
const content=document.querySelector('#content'),statusEl=document.querySelector('#status');
const fmt=n=>new Intl.NumberFormat('ru-RU').format(n||0);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function funnelTable(title,steps){return '<section><h2>'+esc(title)+'</h2><div class="scroll"><table><thead><tr><th>Шаг</th><th>Людей</th><th>От прошлого шага</th><th>От лендинга</th></tr></thead><tbody>'+steps.map((s,i)=>'<tr><td>'+esc(s.label)+'</td><td class="'+(i&&s.from_previous_percent<60?'drop':'good')+'">'+fmt(s.count)+'</td><td>'+s.from_previous_percent.toFixed(1)+'%</td><td>'+s.from_landing_percent.toFixed(1)+'%</td></tr>').join('')+'</tbody></table></div></section>'}
function breakdown(title,rows){if(!rows.length)return '<section><h2>'+esc(title)+'</h2><div class="empty">Данных пока нет.</div></section>';const keys=[['landing','Лендинг'],['cta','Клик'],['group_joined','Вступили'],['test_started','Тест'],['completed','Завершили'],['returned_to_channel','Вернулись']];return '<section><h2>'+esc(title)+'</h2><div class="scroll"><table><thead><tr><th>Сегмент</th>'+keys.map(x=>'<th>'+x[1]+'</th>').join('')+'</tr></thead><tbody>'+rows.map(row=>{const map=Object.fromEntries(row.funnel.map(x=>[x.key,x]));return '<tr><td>'+esc(row.label||'Без названия')+'</td>'+keys.map(([key])=>'<td>'+fmt(map[key]?.count)+'</td>').join('')+'</tr>'}).join('')+'</tbody></table></div></section>'}
function changeHistory(){return '<section><h2>Сделанные изменения</h2><div class="scroll"><table><thead><tr><th>Дата</th><th>Что изменили</th></tr></thead><tbody><tr><td>2 сентября 2026</td><td>Новая воронка: лендинг → вступление в канал → закреплённый тест → четыре экрана результата → возврат в канал. Добавлены отдельные лендинги и атрибуция Meta/YouTube.</td></tr></tbody></table></div></section>'}
async function load(){statusEl.textContent='Загружаю…';content.innerHTML='';const from=document.querySelector('#date-from').value,to=document.querySelector('#date-to').value;try{const r=await fetch('?data=1&date_from='+encodeURIComponent(from)+'&date_to='+encodeURIComponent(to),{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);const d=await r.json();statusEl.textContent='Обновлено '+new Date(d.generated_at).toLocaleString('ru-RU');content.innerHTML=funnelTable('Общая воронка',d.funnel)+breakdown('По источникам',d.by_source)+breakdown('По лендингам',d.by_landing)+breakdown('По кампаниям',d.by_campaign)+breakdown('По объявлениям',d.by_ad)+changeHistory()}catch(e){statusEl.textContent='Не удалось загрузить данные: '+e.message}}
const today=new Date(),from=new Date(today);from.setDate(today.getDate()-6);document.querySelector('#date-to').value=today.toISOString().slice(0,10);document.querySelector('#date-from').value=from.toISOString().slice(0,10);document.querySelector('#filters').addEventListener('submit',e=>{e.preventDefault();load()});load();
</script></body></html>`;
