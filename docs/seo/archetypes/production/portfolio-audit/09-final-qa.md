# Финальный QA корпуса «Архетипы»

Дата: 27 июля 2026.

Режим: независимая post-implementation проверка, без редактирования 26
черновиков, manifest, README и audit-script.

## Вердикт

**PASS**

- P0: **0**.
- P1 в Markdown-корпусе: **0**; прежний P1-MD-01 закрыт в 6 файлах.
- P1 только на runtime/product landing: **1 publication gate**, не дефект
  Markdown-черновиков.
- P2: **1 необязательное редакционное замечание**.

Все условия закрытия Markdown REOPEN выполнены. Runtime/product landing gate
остаётся отдельным условием сквозной publication/E2E-проверки и не переоткрывает
вердикт готовых Markdown-черновиков.

## Закрытие P1-MD-01

### Нейтральные статьи теперь квалифицируют женскую аудиторию `/lightness/#bundle`

Локальная посадочная `/lightness/` прямо определяет «Вкус лёгкости» как
онлайн-неделю **для женщин** (`lightness/index.html`, meta description и hero).
Пакет `/lightness/#bundle` включает эту неделю. Повторная проверка подтвердила:
все шесть статей с гендерно-нейтральным H1, интро и поисковым интентом теперь
прямо квалифицируют формат как женский:

| Файл | Строка CTA |
|---|---:|
| `drafts/01-zhizn-cherez-nado.md` | 119 |
| `drafts/12-ne-znayu-chego-hochu.md` | 139 |
| `drafts/16-ne-umeyu-govorit-net.md` | 219 |
| `drafts/22-sila-bez-kontrolya.md` | 233 |
| `drafts/25-hochyu-no-ne-deystvuyu.md` | 165 |
| `drafts/26-strategiya-bez-vkusa.md` | 173 |

Во всех шести случаях добавлен смысловой gate «вам подходит женский формат» или
его точный эквивалент. Destination `/lightness/#bundle` не изменился;
распределение 8 × 2W сохранено.

`07-ustala-byt-silnoy.md` и `08-khoroshaya-devochka.md` по-прежнему
квалифицируют аудиторию самим H1 и интентом. **P1-MD-01 закрыт.**

## P1 — только runtime/publication gate

### P1-RUNTIME-01. Категоричные promises находятся на destination, не в drafts

Оба mentoring-CTA в Markdown квалифицированы корректно: формат назван женским,
сопровождение не назначается по коду и не обещает результат. Однако
`mentoring/index.html:2683` уже после перехода обещает в категоричной форме:
«Вы перестанете…», «Тело снова начнёт…», «Решения станут…».

Это **не блокер текста 17 или 23** и не требует менять их CTA. Это отдельный
NO-GO для сквозного publication/E2E gate продуктовой страницы: до объявления
полного пути безопасным promises на landing нужно привести к
негарантирующему языку.

## Что прошло

### Главный следующий шаг и manifest

- В каждом из 26 финальных блоков ровно одна ссылка.
- Во всех 26 случаях она точно совпадает с `destination` соответствующего
  asset в `portfolio-routes.json`.
- Фактическое распределение главных шагов:
  - 11 TEST;
  - 8 2W;
  - 2 MENTORING;
  - 3 NAV;
  - 2 ARTICLE.
- `next_intent` и destination согласованы; 2 ARTICLE реализованы как
  `/arhetipy/gestiya/` и `/otnosheniya/zhivu-radi-drugih/`.
- Сырой счётчик audit-script показывает 16 ссылок на тест, потому что включает
  информационные ссылки внутри статей. Это не нарушает распределение главных
  шагов.

### SEO-граф

- Все обязательные ADD/KEEP-связи из
  `08-integrated-implementation-matrix.md` присутствуют.
- Устаревшая связь L01 → R02 удалена.
- Внутри доступного 26-страничного корпуса сирот нет: каждый canonical получает
  хотя бы одну входящую ссылку от другого draft.
- Внешние относительно manifest внутренние targets ограничены утверждёнными
  хабами `/arhetipy/`, `/zhenskie-arhetipy/` и `/arhetipy-method.html`.

### Safety и доверие

- Safety mapping совпадает с матрицей во всех 26 assets:
  3 `normal`, 6 `sensitive`, 17 `critical_route`.
- Полные STOP/профессиональные развилки сохранены; продуктовые CTA стоят после
  самостоятельной пользы и safety-квалификации.
- Прямых ссылок на RETREAT нет.
- В Markdown нет гарантий результата продукта, обещаний изменить партнёра,
  вылечить состояние, гарантировать деньги, успех или правильное решение.
- Оба mentoring-маршрута (`17`, `23`) явно названы женскими и не выводятся
  автоматически из пола или архетипического кода.
- В CTA нет дат, цен, статуса набора, дефицита мест или срочности.
  Динамические даты и цены остаются на продуктовых страницах, как требует
  README.

### Запрещённые элементы

Не найдены:

- Margarita в любом регистре;
- диктовочные, транскрипционные и TODO-маркеры;
- «психологический тест»;
- «ответьте на вопросы» как описание механики теста;
- «узнайте свой главный архетип» и эквивалентные запрещённые формулы;
- прямой `/retreats/` CTA.

### Голос, повторы и H2

- H2 `Один следующий шаг`: **0/26**.
- Сценические входы женских role-pages сохранены.
- Корпус в целом различим по бытовым сценам, ритму и авторским примерам; нет
  серийных вымышленных счастливых финалов.
- Audit-script нашёл 8 групп повторяющихся H2 и 3 группы точных длинных
  абзацев. Повторы длинных абзацев относятся к safety-языку и не являются
  основанием ослаблять инструкции ради стилистического разнообразия.

### JSON/schema

Независимая проверка вернула 0 issues:

- валидный JSON, `schema_version: 1`, ISO-дата;
- 26 assets и 26 существующих source-файлов;
- все обязательные поля заполнены;
- уникальны `route_id`, `source`, `canonical`;
- допустимы `index_state`, `publication_status`, `safety_state`,
  `next_intent`;
- product destination map и пары intent/destination согласованы.

### Автоматические проверки

```text
.\scripts\audit-archetype-portfolio.ps1
PASS: Files=26, ManifestAssets=26, Blockers=0

.\scripts\audit-archetype-portfolio.ps1 -FailOnBlocker
PASS: Blockers=0

git diff --check
PASS
```

`git diff --check` вывел только предупреждения о возможной будущей замене LF на
CRLF; whitespace errors не найдены.

## P2 — необязательные замечания

1. Самые заметные серийные H2 — `Как эту ситуацию описывает язык архетипов`
   (6 статей) и `Когда одной статьи недостаточно` (6 статей). Это не
   acceptance-блокер: каждый повтор занимает менее 30% корпуса, а
   `Один следующий шаг` удалён полностью. При следующем редакционном проходе
   можно разнообразить только несafety-заголовки, не переписывая защитные
   инструкции ради новизны.

## Runtime gates, которые не меняют Markdown-вердикт

- Anchor `/lightness/#bundle` существует локально.
- `/mentoring/` существует локально.
- Локального artefact или rewrite для `/test-arhetipov/` в проверенном checkout
  не найдено. Для 11 TEST-маршрутов это обязательная live-проверка перед
  публикацией, но не дефект draft: README прямо сохраняет assets в статусе
  `draft` до runtime-интеграции.
- Актуальность дат, цен, набора и доступности продукта проверяется на landing в
  момент публикации и не должна копироваться в evergreen CTA.

## Статус после повторной проверки

1. P1-MD-01 исправлен во всех шести CTA без изменения destinations.
2. Финальные CTA повторно сопоставлены с manifest:
   **PASS, 26/26, ровно одна ссылка в каждом финальном блоке**.
3. `.\scripts\audit-archetype-portfolio.ps1 -FailOnBlocker`:
   **PASS, Files=26, ManifestAssets=26, Blockers=0**.
4. `git diff --check`: **PASS**.

Итоговый вердикт Markdown-корпуса: **PASS**.

Перед publication/E2E по-прежнему нужно закрыть P1-RUNTIME-01 и подтвердить
live-доступность `/test-arhetipov/`. Эти runtime-условия не являются дефектами
проверенных Markdown-черновиков.
